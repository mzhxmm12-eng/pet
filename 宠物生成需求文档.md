# 宠物生成需求文档

## 1. 功能目标

网页端宠物生成器用于将用户上传的猫咪或狗狗照片，生成可用于 Windows 桌宠软件的像素宠物素材。

用户上传几张自家宠物照片后，系统生成一套统一风格的像素卡通形象，并输出动作帧、GIF 预览和可导入客户端的宠物资产包。

## 2. 核心功能

### 2.1 图片上传

- 支持上传 1-5 张猫咪或狗狗照片。
- 支持 JPG、PNG、WebP。
- 上传后可预览图片。
- 支持用户选择宠物类型：猫 / 狗。
- 建议提示用户上传清晰正脸、半身、侧面或睡觉照片。

### 2.2 宠物特征识别

- 自动识别宠物主要特征：
  - 宠物类型。
  - 主毛色。
  - 花纹特征。
  - 眼睛颜色。
  - 耳朵、尾巴、体型等明显特征。
- 用户可以编辑或确认识别结果。

### 2.3 像素形象生成

- 根据用户照片和宠物特征，生成像素卡通宠物形象。
- 建议一次生成 4 个候选形象。
- 用户选择最像自己宠物的一版，作为后续动作生成基础。

### 2.4 动作帧生成

需要生成以下动作：

- `idle`：待机，2-4 帧。
- `walk_left`：向左走，4-8 帧。
- `walk_right`：向右走，4-8 帧。
- `sleep`：睡觉，2-4 帧。
- `alert`：注意光标，1-3 帧。
- `play`：扑 / 玩耍，3-6 帧。

生成策略建议：

- 先生成稳定的基础宠物形象。
- 再基于该形象生成动作帧。
- 不建议每一帧完全独立生成，否则容易出现花纹、脸型、颜色不一致。
- `walk_left` 可优先由 `walk_right` 镜像生成，降低生成成本。

### 2.5 GIF 预览

- 每组动作生成一个 GIF 预览。
- 用户可以在网页上直接查看动作效果。
- GIF 仅用于网页预览。
- 正式导出素材建议使用 PNG Sprite Sheet，方便客户端稳定播放。

### 2.6 资产包导出

- 支持导出 `.deskpet` 宠物资产包。
- 资产包包含：
  - `manifest.json`
  - 宠物预览图
  - 动作 GIF 预览
  - PNG Sprite Sheet 动作素材

建议结构：

```text
pet-name.deskpet
  manifest.json
  preview.png
  previews/
    idle.gif
    walk_right.gif
    sleep.gif
    alert.gif
    play.gif
  sprites/
    idle.png
    walk_left.png
    walk_right.png
    sleep.png
    alert.png
    play.png
```

## 3. 技术选型

### 3.1 前端

推荐技术栈：

- `Next.js`
- `React`
- `TypeScript`

主要用途：

- 图片上传和预览。
- 宠物类型选择。
- 特征确认和编辑。
- 候选形象展示。
- GIF 动作预览。
- 资产包下载。

可选库：

- `react-dropzone`：图片拖拽上传。
- `react-easy-crop`：图片裁剪。
- `Tailwind CSS`：页面样式。
- `Canvas`：Sprite Sheet 动画预览。

### 3.2 后端

推荐技术栈：

- `Python`
- `FastAPI`
- `Redis`
- `Celery` 或 `RQ`

主要用途：

- 接收上传图片。
- 创建生成任务。
- 调用生图模型或生图服务。
- 处理图片后处理。
- 生成 GIF。
- 打包 `.deskpet` 文件。

### 3.3 图像处理

推荐技术：

- `Pillow`
- `OpenCV`
- `ffmpeg`

主要用途：

- 图片裁剪和缩放。
- 透明背景处理。
- Sprite Sheet 切帧。
- GIF 合成。
- 图片格式转换。
- 统一画布尺寸和帧数。

### 3.4 生图方案

MVP 推荐优先使用外部图像生成 API，快速验证效果。

后续可使用：

- `ComfyUI`
- `Stable Diffusion`
- 自定义图像生成工作流

推荐生成流程：

```text
用户照片
  -> 宠物主体识别
  -> 宠物特征提取
  -> 生成基础像素形象
  -> 用户选择候选
  -> 基于候选生成动作帧
  -> 后处理为 Sprite Sheet
  -> 生成 GIF 预览
  -> 打包 .deskpet
```

## 4. MVP 范围

### 必做

- 上传宠物图片。
- 生成像素宠物候选形象。
- 选择一个候选形象。
- 生成 6 组动作帧。
- 生成 GIF 预览。
- 导出 `.deskpet` 资产包。

### 暂不做

- 完整像素画编辑器。
- 社区分享。
- 多宠物合成。
- 复杂配饰系统。
- 账号和付费体系。

