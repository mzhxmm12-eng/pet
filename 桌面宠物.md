# 桌面宠物需求文档

## 1. 产品目标

开发一款 Windows 桌面宠物小程序。MVP 阶段先内置一只像素猫，验证桌宠常驻桌面、基础动作、光标互动、文件投喂和资产规范。

暂不考虑从网站生成素材导入宠物，先制作一个标准桌宠，形成后续自定义桌宠导入规范。

## 2. 核心功能

### 2.1 桌宠基础展示

- 桌宠常驻 Windows 桌面。
- 支持透明背景、无边框窗口。
- 支持拖拽移动宠物。
- 支持记住上次位置。
- 支持右键菜单：隐藏、暂停、设置、退出。
- 支持托盘图标和托盘菜单。

### 2.2 基础行为

- `idle`：待机，眨眼、尾巴轻微摆动。
- `walk_left`：向左走动。
- `walk_right`：向右走动。
- `sleep`：睡觉，长时间无操作后进入。
- 宠物会在桌面范围内随机走动，不能走出屏幕边界。
- 用户靠近、点击、拖拽文件时可以唤醒睡觉中的宠物。

### 2.3 光标逗猫

- 光标靠近宠物时，宠物进入警觉状态。
- 光标快速经过宠物附近时，宠物触发扑一下或玩耍动作。
- 点击宠物时，宠物播放反馈动作。
- 互动结束后，宠物恢复自动行为。

### 2.4 文件投喂

- 用户可以把废弃文件拖到宠物身上。
- 宠物把文件当作猫粮，播放吃东西动画。
- 投喂完成后，文件进入 Windows 回收站。
- 默认投喂前需要确认，避免误删。
- 移动失败时不删除原文件，并提示失败原因。
- 支持单个文件、多个文件、文件夹投喂。
- 拒绝系统目录、应用目录、不可访问路径等危险对象。

### 2.5 设置功能

- 显示 / 隐藏宠物。
- 暂停 / 继续活动。
- 调整宠物大小。
- 调整动画速度。
- 开启 / 关闭点击穿透。
- 开启 / 关闭文件投喂。
- 开启 / 关闭投喂前确认。
- 重置宠物位置。
- 设置开机启动。

## 3. 宠物资产规范

### 3.1 内置宠物

MVP 先制作一只内置像素猫：

- 类型：猫。
- 风格：2D 可爱像素风。
- 画布：64x64。
- 背景：透明。
- 默认朝向：向右。

### 3.2 必备动作

- `idle`：待机。
- `walk_left`：向左走。
- `walk_right`：向右走。
- `sleep`：睡觉。
- `alert`：光标靠近时警觉。
- `play`：扑光标或点击反馈。
- `eat`：吃文件猫粮。
- `reject`：投喂失败或拒绝。

### 3.3 资产目录

```text
assets/pets/orange-cat/
  manifest.json
  preview.png
  sprites/
    idle.png
    walk_left.png
    walk_right.png
    sleep.png
    alert.png
    play.png
    eat.png
    reject.png
```

### 3.4 `.deskpet` 资产包

后续自定义桌宠统一使用 `.deskpet` 资产包。本质是 zip 文件，内部包含：

```text
pet-name.deskpet
  manifest.json
  preview.png
  sprites/
    idle.png
    walk_left.png
    walk_right.png
    sleep.png
    alert.png
    play.png
    eat.png
    reject.png
```

`manifest.json` 示例：

```json
{
  "formatVersion": 1,
  "petId": "orange_cat_builtin",
  "name": "Orange",
  "species": "cat",
  "style": "pixel_cute",
  "canvas": {
    "width": 64,
    "height": 64,
    "scale": 2
  },
  "animations": {
    "idle": {
      "file": "sprites/idle.png",
      "frameWidth": 64,
      "frameHeight": 64,
      "frames": 4,
      "fps": 4,
      "loop": true
    }
  }
}
```

## 4. 技术选型

### 4.1 客户端

推荐使用：

- `.NET 8/9`
- `WPF`
- `Win32 API`

原因：

- WPF 适合做 Windows 原生透明窗口、托盘菜单和设置页。
- Win32 API 方便处理点击穿透、置顶、多显示器、全局鼠标位置、拖拽文件等桌宠能力。
- 相比 Electron，WPF 更轻量，更适合常驻桌面运行。

### 4.2 渲染方案

- MVP 使用 WPF 图片渲染。
- 宠物动画使用 PNG Sprite Sheet。
- 客户端通过 `manifest.json` 读取动作帧、帧率和循环方式。
- 后续性能不足时，可升级到 `SkiaSharp`。

### 4.3 本地存储

推荐使用：

- `settings.json` 保存用户设置。
- 本地文件夹保存宠物素材。
- 后续需要宠物列表、投喂记录时再引入 `SQLite`。

建议目录：

```text
%AppData%/DeskPet/
  pets/
    {petId}/
      manifest.json
      preview.png
      sprites/
  settings.json
```

### 4.4 回收站能力

文件投喂必须调用 Windows 系统回收站能力，不能永久删除文件。

推荐实现：

- `Microsoft.VisualBasic.FileIO.FileSystem.DeleteFile`
- `Microsoft.VisualBasic.FileIO.FileSystem.DeleteDirectory`
- `RecycleOption.SendToRecycleBin`

也可以封装 Win32 `IFileOperation` 或 `SHFileOperation`。

## 5. 推荐项目分层

```text
DeskPet.App
  应用启动、托盘、设置窗口

DeskPet.Presentation
  透明宠物窗口、动画渲染、右键菜单

DeskPet.Domain
  宠物状态机、行为规则、投喂规则

DeskPet.Assets
  manifest 解析、Sprite Sheet 校验、资产加载

DeskPet.Platform.Windows
  Win32 能力、回收站、全局鼠标、多显示器

DeskPet.Storage
  设置保存、宠物位置保存、本地文件管理
```

## 6. MVP 开发顺序

1. 创建 WPF 透明无边框宠物窗口。
2. 制作或放入内置像素猫素材。
3. 实现 Sprite Sheet 动画播放。
4. 实现 `idle`、`walk`、`sleep` 状态机。
5. 实现拖拽移动和位置保存。
6. 实现托盘菜单和右键菜单。
7. 实现光标靠近、点击、快速移动互动。
8. 实现文件拖拽投喂和进入回收站。
9. 实现基础设置页。
10. 将内置宠物整理成 `.deskpet` 规范，为后续自定义导入做准备。
